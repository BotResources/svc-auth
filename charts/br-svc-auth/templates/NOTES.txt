br-svc-auth {{ .Chart.AppVersion }} installed in namespace {{ .Release.Namespace }}.

Service: {{ include "br-svc-auth.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.port }}

Endpoints (in-cluster):
  POST   http://{{ include "br-svc-auth.fullname" . }}:{{ .Values.port }}/auth/token
  POST   http://{{ include "br-svc-auth.fullname" . }}:{{ .Values.port }}/auth/refresh
  GET    http://{{ include "br-svc-auth.fullname" . }}:{{ .Values.port }}/auth/check
  POST   http://{{ include "br-svc-auth.fullname" . }}:{{ .Values.port }}/auth/logout
  GET    http://{{ include "br-svc-auth.fullname" . }}:{{ .Values.port }}/health

{{- if not .Values.oidcProviders }}

WARNING: no OIDC providers configured. /auth/token will reject every id_token —
         there is no bypass. Declare at least one provider in oidcProviders[]
         (for local/e2e stacks, deploy the br-oidc-test-idp fixture from
         br-e2e-harness and declare it like any provider).
{{- end }}

JWT signing secret must exist in Kubernetes Secret "{{ .Values.jwt.secret.existingSecret }}"
under key "{{ .Values.jwt.secret.existingSecretKey }}". The chart does not create it.
