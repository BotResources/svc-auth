apiVersion: v1
kind: Service
metadata:
  name: {{ include "br-svc-auth.fullname" . }}
  labels:
    {{- include "br-svc-auth.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "br-svc-auth.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
